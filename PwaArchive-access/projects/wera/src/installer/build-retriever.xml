<project name="webmodulebuilder" default="compile" basedir=".">
    <description>
	  Build file WERA arcretriever
    </description>

  <property name="extralib" value="extralib"/>

	<!-- set global properties for this build -->

  <property name="webappsourcedir" value="cvsexport/archive-access/projects/wera/src/webapps/arcretriever" />
	<property name="module" value="arcretriever" />
	<property name="src" value="cvsexport/archive-access/projects/wera/src/java" />
	<property name="lib" value="cvsexport/archive-access/projects/wera/lib" />

  <copy todir="${lib}">

    <fileset dir="${extralib}">
      <!-- additional lib needed to build the arcretriever (servlet-2.3.jar).
      Don't know why this is not part of wera lib. What you say stack ? -->
	  	<include name="servlet-2.3.jar" />
    </fileset>      
	</copy>  
  
	<property name="dist" value="arcretriever" />
	<property name="warfile" value="${module}.war" />

    <target name="clean">
        <delete file="${warfile}" failonerror="false" />
        <delete dir="${dist}" failonerror="false" />   
    </target>
    
	<target name="init">
		<!-- Create the dist directory structure used by compile
         and copy the deployment descriptors into it-->
		<mkdir dir="${dist}" />
		<mkdir dir="${dist}/images" />
		<mkdir dir="${dist}/WEB-INF" />
		<mkdir dir="${dist}/WEB-INF/classes" />
		<mkdir dir="${dist}/WEB-INF/lib" />
    
		<copy todir="${dist}">
			<fileset dir="${webappsourcedir}">
				<include name="**/*.jsp" />
				<include name="**/*.css" />
				<include name="LICENSE.TXT" />
			</fileset>
		</copy>
		<copy todir="${dist}/WEB-INF">
			<fileset dir="${webappsourcedir}/WEB-INF">
				<include name="web.xml" />
				<include name="arcretriever.properties" />
			</fileset>
		</copy>
		<copy todir="${dist}/WEB-INF/lib">
			<fileset dir="${lib}">
				<include name="*.jar" />
			</fileset>
			<fileset dir="${extralib}">
				<include name="servlet-2.3.jar" />
			</fileset>      
		</copy>
		<copy todir="${dist}/images">
			<fileset dir="${webappsourcedir}/images">
				<include name="*.jpg" />
				<include name="*.png" />
				<include name="*.gif" />
			</fileset>
		</copy>
	</target>

	<target name="compile" depends="init">
		<javac destdir="${dist}/WEB-INF/classes" srcdir="${src}">
			<classpath>
				<fileset dir="${lib}">
					<include name="**/*.jar" />
				</fileset>
			</classpath>
		</javac>
	</target>

  <!-- Move this to installer, because installer should update web.xml
	<target name="war" depends="compile">
		<jar jarfile="${warfile}" basedir="${dist}" />
	</target> 
  -->
</project>
